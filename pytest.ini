[pytest]
testpaths = tests
markers =
    network: tests that reach a live public SPARQL endpoint (dbpedia) — excluded from the default gate
    e2e: end-to-end tests that boot a real MLflow server (slower)
